name: Client CI

on:
    pull_request:
        paths:
            - "fogcms-client/**"

jobs:
    install:
        name: Install
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: fogcms-client
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Cache node_modules
              uses: actions/cache@v4
              with:
                  path: node_modules
                  key: client-ci-install-${{ github.run_id }}-${{ hashFiles('package-lock.json') }}

            - name: Install dependencies
              run: npm ci

    format:
        name: Format
        runs-on: ubuntu-latest
        needs: install
        defaults:
            run:
                working-directory: fogcms-client
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Cache node_modules
              uses: actions/cache@v4
              with:
                  path: node_modules
                  key: client-ci-format-${{ github.run_id }}-${{ hashFiles('package-lock.json') }}

            - name: Run format check
              run: npm run format:check

    lint:
        name: Lint
        runs-on: ubuntu-latest
        needs: format
        defaults:
            run:
                working-directory: fogcms-client
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Cache node_modules
              uses: actions/cache@v4
              with:
                  path: node_modules
                  key: client-ci-lint-${{ github.run_id }}-${{ hashFiles('package-lock.json') }}

            - name: Run lint
              run: npm run lint

    test:
        name: Tests
        runs-on: ubuntu-latest
        needs: lint
        defaults:
            run:
                working-directory: fogcms-client
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Cache node_modules
              uses: actions/cache@v4
              with:
                  path: node_modules
                  key: client-ci-test-${{ github.run_id }}-${{ hashFiles('package-lock.json') }}

            - name: Run tests
              run: npm run test -- --watch=false --browsers=ChromeHeadless

    build:
        name: Build
        runs-on: ubuntu-latest
        needs: test
        defaults:
            run:
                working-directory: fogcms-client
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "20"

            - name: Cache node_modules
              uses: actions/cache@v4
              with:
                  path: node_modules
                  key: client-ci-build-${{ github.run_id }}-${{ hashFiles('package-lock.json') }}

            - name: Build production
              run: npm run build -- --configuration production
